<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

#<match jarcicd.**>
#  @type stdout
# </match>


# Match events tagged with "datadog.**" and
# send them to Datadog
<match jarcicd.**>

  @type datadog
  @id awesome_agent
  api_key 225fc00f115a89b0568771b8aba0cb50


  <buffer>
          @type memory
          flush_thread_count 4
          flush_interval 3s
          chunk_limit_size 5m
          chunk_limit_records 500
  </buffer>

</match>